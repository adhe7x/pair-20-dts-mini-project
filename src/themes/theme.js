import { createTheme } from '@mui/material/styles';

const theme = createTheme ({
  palette: {
    type: 'dark',
    primary: {
      main: '#c71111',
    },
    secondary: {
      main: '#11bb03',
    },
    background: {
      default: '#252424',
    },
  },
});

export default theme;